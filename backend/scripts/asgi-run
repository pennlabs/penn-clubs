#!/bin/bash

# Django Migrate
/usr/local/bin/python3 /app/manage.py migrate --noinput

# Switch to project folder
cd /app/

# Run Daphne
exec /usr/local/bin/uvicorn --host 0.0.0.0 --port 80 --workers 3 pennclubs.asgi:application
