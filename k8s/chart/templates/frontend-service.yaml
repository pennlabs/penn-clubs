apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-frontend-svc
{{ if .Values.staging }}
  namespace: staging
{{- end }}
spec:
  ports:
    - port: {{ .Values.frontend.port }}
      targetPort: 80
  selector:
    name: {{ .Release.Name }}-frontend